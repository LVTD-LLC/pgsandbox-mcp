{
  "name": "pgsandbox-mcp",
  "version": "0.1.1",
  "description": "MCP server for disposable Postgres sandboxes.",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "check": "cargo check",
    "package:homebrew": "cargo build --release && sh scripts/package-homebrew.sh",
    "package:release": "cargo build --release && sh scripts/package-release.sh",
    "test": "cargo test",
    "typecheck": "cargo check",
    "start": "cargo run --",
    "site:check-changelog": "node scripts/check-site-changelog.mjs",
    "site:check-blog-content": "node scripts/check-site-blog-content.mjs",
    "site:check-blog-tables": "node scripts/check-site-blog-tables.mjs",
    "site:build": "npm --prefix site run build"
  },
  "keywords": [
    "mcp",
    "postgres",
    "postgresql",
    "agents",
    "sandbox"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LVTD-LLC/pgsandbox-mcp.git"
  },
  "license": "MIT"
}
